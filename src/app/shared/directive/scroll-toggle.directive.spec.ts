import { ScrollToggleDirective } from './scroll-toggle.directive';

describe('ScrollToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToggleDirective();
    expect(directive).toBeTruthy();
  });
});
